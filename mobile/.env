EXPO_PUBLIC_SUPABASE_URL=https://ljpgflhmevmghfvcacnk.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_bTJAVzA2isSKOxm8_KIE1w_7noApAHd

# The Next.js web app's URL — hosts the /api/invigilator/* routes this app
# calls. Use your machine's LAN IP (not localhost) when testing on a real
# phone over Expo Go, since "localhost" from the phone means the phone
# itself, not your dev machine. localhost is fine for `expo start --web`.
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.4:3000
